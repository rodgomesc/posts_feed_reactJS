import React, { Fragment } from "react";
import { Container } from "./styles";

const Header = ({ params }) => (
  <Container>
    <h1>RocketBook</h1>
  </Container>
);

export default Header;
